ORG 0000H
	MOV DPTR, #300H
	MOV R2, #5
	MOV R0, #30H
;-------------------STORING IN RAM FIRST------
	AGAIN: CLR A
	MOVC A, @A+DPTR
	MOV @R0, A
	INC R0
	INC DPTR
	DJNZ R2, AGAIN
;--------CHECK PALINDROME----------
	MOV R0, #30H
	MOV R1, #36H
	MOV R2, #3
	
	AGAIN: MOV A, @R0
	MOV B, @R1
	CJNE A, B, NEXT
	INC R0
	DEC R1
	DJNZ R2, AGAIN
	MOV P1, #'Y'
	
	NEXT: NOP
	
ORG 0300H
	DB 'MADAM',0
END